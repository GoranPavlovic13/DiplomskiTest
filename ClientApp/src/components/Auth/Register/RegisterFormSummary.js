import classes from './RegisterFormSummary.module.css'

const RegisterFormSummary = () => {
    return (
        <section className={classes.summary}>
          <h2>Register</h2>
          <p>
            Create an account to start learning programming languages and track your progress.
          </p>
        </section>
      );
};

export default RegisterFormSummary;